using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Target : MonoBehaviour
{

	public bool inside_grid;
    // Start is called before the first frame update
    void Start()
    {
        inside_grid = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
